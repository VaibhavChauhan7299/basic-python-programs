# A built-data type that lets us create immutable sequences of values.

#tup = (2, 4, 5, 7,)
# print(tup[0])
# print(tup[1])
#print(type(tup))

#tuple methods

#tup = (1, 2, 3, 4, 5, 6, 7, 8, 9, 9,)
#print(tup.index(3)) #returns index of first occurrence
#print(tup.count(9)) #counts total occurrences

#write to count the number of students with the "A" grade in the following tuple.

#grade = ("c", "D", "A", "A", "B", "B", "A")
#print(grade.count("A"))


#store the above value in a list & sort them from "A" to "D".

# list = ["C", "D", "A", "A", "B", "B", "A"]
# list.sort() 
# print(list)

